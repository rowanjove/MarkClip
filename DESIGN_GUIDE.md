# 页摘界面与品牌规范

这份规范用于 Popup、页面快捷入口和选择区域提示的后续设计与代码审查。目标是让“页摘”保持一个安静、可预期的本地工具形象，而不是把界面做成营销卡片或聊天入口。

## 品牌边界

- 对外名称：**页摘**
- 固定副标题：**网页摘录为 Markdown**
- 核心承诺：在浏览器本地把网页内容摘录成 Markdown。
- 内部兼容：JavaScript 全局对象仍使用 `MarkClip*`，本地设置键仍使用 `page2md:*`。
- 图标语义：从页面中摘出一行。墨绿色圆角底承载米白色开放页面轮廓，中间暖金色摘录向右移出；不使用折角、机器人、星光、对话气泡或魔法棒。
- 图标按 16px 网格构造，最小尺寸的主体笔画为 2px；保留外围透明留白。金色 `#e7bc69` 仅用于品牌图标，不改变界面操作的绿色强调色。

## 视觉基线

### 色彩

| 用途 | 深色 | 浅色 |
| --- | --- | --- |
| 页面背景 | `#202522` | `#f6f7f3` |
| 内容表面 | `#292f2b` | `#ffffff` |
| 次级表面 | `#252b27` | `#eef2ed` |
| 边框 | `#424b45` | `#d4ddd5` |
| 正文 | `#f4f6f2` | `#1e2721` |
| 次要文字 | `#aeb8b0` | `#58675d` |
| 强调色 | `#6fba84` | `#24623d` |

强调色只用于当前状态、主操作、选区边框和焦点提示。不要为了“科技感”增加蓝紫渐变、发光描边或大面积透明叠层。

新安装默认使用浅色主题：它更接近浏览器原生工具的阅读环境，正文与边界在 360px Popup 中更容易扫读。已有用户保存的深色主题继续保留；真实用户反馈进入下一次迭代时只复核这一决定，不自动覆盖个人设置。

### 尺寸与形状

- Popup 宽度固定为 `360px`，内容区独立滚动，底部操作栏始终可见。
- 页面浮窗面板最大宽度为 `304px`，根据视口自动翻转并限制最大高度。
- 普通按钮圆角不超过 `6px`；面板圆角不超过 `9px`。开关使用胶囊形是语义例外。
- 使用 `1px` 分隔线建立层级，阴影只承担与页面背景分离的作用，不制造光晕。
- 默认使用系统字体；Markdown 预览使用等宽字体。

## 组件行为

### Popup

- 首屏只显示正文、选择区域、整页、图片处理和复制/保存/重新提取。
- Obsidian 与批量保存放入默认折叠的“更多操作”。
- 转换中所有可能重复触发的控件禁用，状态区显示忙碌状态和取消入口。
- 设置变化或标签页地址变化后，旧预览不能直接复用。
- 预览文本和文件标题可编辑，编辑器内的原生复制快捷键必须保留。

### 页面快捷入口

- 未打开时只显示摘录图标；打开后才显示操作面板。
- 普通点击打开/关闭，拖动只改变位置，不触发打开动作。
- 面板不得出现在视口外；Escape 关闭面板并把焦点还给入口按钮。
- 同一页面最多存在一个浮窗实例，设置变化要立即同步到已有实例。

### 选择区域

- 页面遮罩只降低背景对比度，选区用低饱和绿色边框和轻微填充表示。
- 提示条固定使用“选择要保留的区域”“完成”“取消”等具体动作词。
- 只处理提示条内部的 `data-action`，页面自身的同名属性不能触发扩展操作。
- 空白 `body`、文档根节点和扩展自己的浮窗不能被当成选区。

## 无障碍与动效

- 所有操作控件使用真实 `button`、`input`、`textarea` 或 `summary` 元素。
- 状态和错误使用 `aria-live`，转换期间根节点标记 `aria-busy`。
- 键盘顺序覆盖范围、图片处理和导出动作；焦点环必须清晰可见。
- Escape 关闭浮层或收起更多操作；编辑器获得焦点时不拦截浏览器原生快捷键。
- `prefers-reduced-motion: reduce` 下关闭脉冲、位移和过渡效果。

## 资产与验收

- 图标源代码：`scripts/generate-icons.js`。
- 必须生成并检查 `icons/icon16.png`、`icons/icon48.png`、`icons/icon128.png` 的实际尺寸。
- `manifest.json` 的顶层 `icons` 和 `action.default_icon` 必须指向同一组文件。
- 视觉改动至少复核：深/浅主题、Popup 窄视口、浮窗靠近四条边、键盘路径、减少动效偏好和选择区域空白点击。

常用检查命令：

```bash
npm run icons:generate
npm run check
$env:RUN_BROWSER_E2E='1'; npm run test:e2e
```
