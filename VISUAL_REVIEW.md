# 页摘视觉验收记录

## 本轮记录

- 日期：2026-08-31
- 范围：Popup、页面快捷入口、选择区域提示、图标和主题默认值
- 结论：通过内部验收；新安装默认浅色主题，已有用户保存的主题不变。
- 2026-09-01 图标更新：替换为“从页面中摘出一行”的绿色/米白/暖金图标，重新生成四张界面截图，并复核浅色 Popup 与深色浮窗中的显示。10 项资源与 manifest 测试、66 个文件语法检查通过；本次仅涉及品牌资源，未重复完整功能回归。

## 截图证据

| 场景 | 文件 | 验收结果 |
| --- | --- | --- |
| Popup 浅色 | `visual-regression/popup-light.png` | 360×600，底部操作栏完整可见，内容区无横向滚动 |
| Popup 深色 | `visual-regression/popup-dark.png` | 360×600，正文与次要文字层级清晰 |
| 浮窗浅色 | `visual-regression/floating-light.png` | 1200×760，面板贴近右下角且不遮挡主要内容 |
| 浮窗深色 | `visual-regression/floating-dark.png` | 1200×760，深色面板在浅色网页背景上的边界和主按钮可辨认 |

截图由 `npm run ui-review:capture` 生成，供人工视觉复核；`test/ui-assets.test.js` 仅校验尺寸，不做像素差异比较。浮窗截图模拟偏好和操作消息，图标使用真实扩展资源地址并等待解码成功后截图，不使用 data URL 替代。

## 本次审查修复验收

2026-09-01 上传前复验：重新生成图标、商店素材和四张截图，人工复核浅色 Popup 与浮窗；`npm run check` 为 83 项（80 通过，3 项浏览器检查默认跳过），同时启用 `RUN_BROWSER_E2E=1` 与 `RUN_CHROME_CDP_E2E=1` 后 `npm run test:e2e` 为 4/4 通过、无跳过。第三方资源检查及 `npm audit` 通过，报告零漏洞。此结果对应本地 Windows 环境，GitHub Linux CI 单独执行。

| 问题 | 修复与证据 |
| --- | --- |
| 准备提取期间 SPA 跳转混用来源 | 异步准备结束后验证 URL，再同步捕获元数据和内容；集成测试覆盖跳转中止与同 URL 更新 |
| 小视口浮窗面板越界 | 按可用空间限制高度并夹紧坐标；Chromium 覆盖 360×400、320×240、768×600、360×100，各测四角与中心，并检查拖动中边界 |
| 隐藏后被延迟初始化重新创建 | 使用可失效的初始化代次；测试覆盖主动隐藏、无宿主时收到隐藏事件，以及隐藏后重新显示 |
| 网页浮窗图标无法加载 | 仅公开 `icons/icon48.png`；Chromium 验证真实扩展 URL 解码成功且固有宽度为 48 |

浏览器浮窗测试还验证 Escape 关闭及焦点恢复，但使用模拟偏好存储，不代表完整权限申请、复制和下载链路已完成浏览器验收。

## 交互验收

- [x] Popup 打开后焦点进入正文范围按钮。
- [x] Popup 的“更多操作”默认收起，Escape 可以收起。
- [x] 转换忙碌时重复操作被禁用，状态区标记 `aria-busy`。
- [x] 编辑器获得焦点时保留浏览器原生 Ctrl/Cmd+C。
- [x] 浮窗普通点击打开/关闭，拖动不会误触发打开。
- [x] 浮窗 Escape 关闭面板并恢复入口焦点。
- [x] 浮窗位置在四边和窄视口内保持可见。
- [x] 选择区域点击 `body` 或扩展自身界面不会产生错误选区。

## 无障碍验收

- [x] 操作控件使用原生按钮、开关、输入框和 `summary`。
- [x] 状态、错误和 Toast 具有可读的 live region。
- [x] 主题切换、范围选择和减少动效规则有对应状态表现。
- [x] 深浅主题使用统一焦点环和低饱和绿色强调色。

目标商店上传后的裁切效果、Edge 实机表现和第二位使用者的独立体验仍属于外部发布验收，不在本地代码门禁内完成。
